<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="LikeHeroToZeroPersistenceUnit">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
            <class>test.Emission</class>        	
        	<class>test.Benutzer</class>
   		   

        <properties>
        	<!-- ermöglicht die Verbindung zu MySQL -->
            <property name="javax.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/> 
            <!--gibt die lokale Datenbank an -->
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/lh2z"/>
            <!-- User -->
            <property name="javax.persistence.jdbc.user" value="root"/>
            <!-- Passwort -->
            <property name="javax.persistence.jdbc.password" value=""/>
            <!-- Hibernate erzeugt SQL-Befehle , die mit MySQL kompatibel sind -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>
            <!-- überprüft beim Start, ob Tabellenstruktur der Datenbank mit den JPA-Entitäten übereinstimmt -->
            <property name="hibernate.hbm2ddl.auto" value="validate"/>
        </properties>
    </persistence-unit>
</persistence>